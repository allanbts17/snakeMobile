extends Node2D


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	#print(Global.pos)
	pass # Replace with function body.

func _draw():
	#var pos = Global.board_offset + Global.square_size/2
	#var c = Color(0.5,0.5,0.5)
	#print(Global.pos[Vector2(0,0)])
	#for x in Global.board_squares.x:
	#	for y in Global.board_squares.y:
			#pos = Global.board_offset + Vector2((x+1)*Global.square_size.x,(y+1)*Global.square_size.y) - Global.square_size/2
			
	#		print(pos)
	#		draw_circle(Global.pos[Vector2(x,y)],5,c)
		#pos.x += 50
# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
	pass
